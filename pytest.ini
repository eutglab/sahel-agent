[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
markers =
    offline: must pass with no network access
    integration: exercises real external providers (skipped unless configured)
