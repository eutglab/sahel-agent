# SAHEL Agent — runtime dependencies
# Every dependency has a reason. No heavy infra.
# Pinned to exact versions verified end-to-end (fresh venv, full import chain,
# full test suite) — reproducible deploys instead of "works locally" drift.

streamlit==1.63.0      # UI (single page, zero frontend build)
pydantic==2.13.5       # strict tool input/output contracts (anti-hallucination)
pyyaml==6.0.3          # configurable sensor thresholds + defaults
pillow==12.3.0         # local image heuristic (no ML model)
numpy==2.2.6           # pixel math for the local vision heuristic
pandas==2.3.3          # benchmark tables + run history views
requests==2.34.2       # Open-Meteo / Exa (keyless-capable) HTTP calls

# --- optional, only used when a real LLM provider is enabled ---
# anthropic>=0.40      # AnthropicLLMClient (real reasoning + vision)
# openai>=1.30         # OpenAILLMClient (alternative provider)

# --- dev / test ---
pytest>=7.4
